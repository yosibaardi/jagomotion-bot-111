export function assertInstalled(cmd, name, code) {}

export const customDispatcher = undefined;

export function compressMessage(msg) {
    return msg;
}

export function decompressMessage(msg) {
    return msg;
}
